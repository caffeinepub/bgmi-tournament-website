{
  "frontend/src/App.tsx": "Main application entry point that sets up TanStack Router with all routes (home, register, login, tournaments, dashboard, terms, admin login, admin dashboard), wraps the route tree with AuthProvider and AdminAuthProvider contexts, and exports the RouterProvider.",
  "frontend/src/main.tsx": "Application bootstrap that renders the React app with QueryClientProvider and InternetIdentityProvider wrapping the App component.",
  "frontend/src/pages/TermsPage.tsx": "Terms & Conditions page with a vibrant colorful content section — headings cycle through orange/cyan/pink/gold/lime/purple neon colors with glowing left-border accents, paragraphs and bullet points each get distinct vivid colors, and a rainbow gradient bar frames the content block. The rest of the page layout (header, footer, loading state) remains unchanged.",
  "frontend/src/pages/AdminLoginPage.tsx": "Two-phase credential-based admin login page — Phase 1 validates Admin ID \"Empire Esports\" / Password \"Shivam803119&\", and Phase 2 validates Admin ID \"6299724471\" / Password \"6202497864\"; both phases must pass before `adminLogin()` is called and the user is redirected to /admin/dashboard.",
  "frontend/src/pages/AdminDashboardPage.tsx": "Full admin dashboard with collapsible sidebar, six tabs (Tournaments, Registrations, Players, Terms, Support, Social), using the correct `logout` from `AdminAuthContext` and correct hook imports (`useGetRegistrationsForTournament`, `useCloseSupportTicket`). Supports Daily and Mega tournament creation with separate form fields.",
  "frontend/src/components/AdminProtectedRoute.tsx": "Guards admin routes by checking `isAdminAuthenticated` from AdminAuthContext, redirecting to `/admin` if not authenticated.",
  "frontend/src/components/ProtectedRoute.tsx": "Guards player routes by checking `isAuthenticated` from AuthContext, redirecting to `/player/login` if not authenticated.",
  "frontend/src/components/Footer.tsx": "Site footer with social links (fetched via `useGetSocialLinks`), navigation buttons using `useNavigate` (no `Link` component to avoid route type errors), and caffeine.ai attribution.",
  "frontend/src/components/Header.tsx": "Sticky site header with logo, desktop/mobile navigation for Tournaments, Terms, Login/Register/Logout, and a subtle \"Admin\" link (text-[10px] text-gray-600) that navigates to /admin but is visually inconspicuous so regular users overlook it.",
  "frontend/src/pages/TournamentsPage.tsx": "Public tournaments listing page using `useGetAllTournaments` and `isAuthenticated`, with tournament cards showing entry fee/prize/slots/map, register button opening PaymentModal, and correct `/player/login` and `/player` navigation routes.",
  "frontend/src/index.css": "Global styles with OKLCH color system, Orbitron/Rajdhani/Exo2 font imports, dark background defaults, scrollbar styling, and brand gradient utilities. Adds `.font-orbitron` utility class for gaming-style headings.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file for all backend operations including tournaments, registrations, players, wallet, payment proofs, withdrawals, support tickets, social links, terms, and prize distribution. Fixed `useUpdateRegistrationStatus` to avoid importing the non-exported `RegistrationStatus` type by using a local string union type with a safe cast.",
  "frontend/tailwind.config.js": "Tailwind config with brand colors (`brand-red: #C0100A`, `brand-orange: #FF6A00`, `brand-dark`, `brand-darker`), Orbitron font family, brand gradient background image, and brand shadow utilities.",
  "frontend/src/context/AdminAuthContext.tsx": "Simplified admin authentication context that manages a single boolean `isAdminAuthenticated` state (persisted in localStorage), exposing `adminLogin()` to set it true and `logout()` to clear it — no Internet Identity dependency.",
  "frontend/src/pages/RegisterPage.tsx": "Three-step registration page (Details → Authenticate → Success) where Step 2 now shows a single \"Connect with Internet Identity\" button (replacing AuthMethodButtons) that directly calls iiLogin(), with clean state reset on step entry, no stuck \"Connecting...\" state, and inline error handling that resets the button to clickable after failure.",
  "frontend/src/pages/LoginPage.tsx": "Login page with a single \"Connect with Internet Identity\" button (replacing AuthMethodButtons) that directly calls iiLogin(), with proper loading states, error display, and post-authentication profile fetch and redirect logic.",
  "frontend/src/pages/DashboardPage.tsx": "Full player dashboard with sidebar navigation (Profile, Tournaments, Wallet, Withdraw, Support tabs), using `useAuth()` hook correctly instead of the non-exported `AuthContext` directly. Includes coin wallet management, payment proof upload, withdrawal requests, tournament joining with coins, and support ticket creation.",
  "frontend/src/components/AuthMethodButtons.tsx": "Reusable authentication method buttons component that renders Google, Apple, and Microsoft buttons in a 3-column row plus a full-width \"Continue with Passkey\" button below, matching the id.ai \"Choose method\" layout. Each button calls the same `onMethodClick` callback which triggers the Internet Identity login flow, with a unified loading state."
}