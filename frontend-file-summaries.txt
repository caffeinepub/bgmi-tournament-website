{
  "frontend/src/App.tsx": "Main application entry point that sets up TanStack Router with all routes (home, register, login, tournaments, dashboard, terms, admin login, admin dashboard), wraps the route tree with AuthProvider and AdminAuthProvider contexts, and exports the RouterProvider.",
  "frontend/src/main.tsx": "Application bootstrap that renders the React app with QueryClientProvider and InternetIdentityProvider wrapping the App component.",
  "frontend/src/pages/TermsPage.tsx": "Terms & Conditions page with a vibrant colorful content section — headings cycle through orange/cyan/pink/gold/lime/purple neon colors with glowing left-border accents, paragraphs and bullet points each get distinct vivid colors, and a rainbow gradient bar frames the content block. The rest of the page layout (header, footer, loading state) remains unchanged.",
  "frontend/src/pages/AdminLoginPage.tsx": "Admin-only login page at /admin with hardcoded credential validation (username: 'Empire Esports', password: 'Shivam803119&'), redirects to /admin/dashboard on success.",
  "frontend/src/pages/AdminDashboardPage.tsx": "Full admin dashboard with red-orange gradient sidebar, top bar, and six tabbed sections (Tournaments, Registrations, Players, Terms, Support Tickets, Social Links). Removed the conflicting local `FileText` function declaration that clashed with the lucide-react import.",
  "frontend/src/components/AdminProtectedRoute.tsx": "Guards admin routes by checking `isAdminAuthenticated` from AdminAuthContext, redirecting to `/admin` if not authenticated.",
  "frontend/src/components/ProtectedRoute.tsx": "Guards player routes by checking `isAuthenticated` from AuthContext, redirecting to `/player/login` if not authenticated.",
  "frontend/src/components/Footer.tsx": "Site footer with social links (fetched via `useGetSocialLinks`), navigation buttons using `useNavigate` (no `Link` component to avoid route type errors), and caffeine.ai attribution.",
  "frontend/src/components/Header.tsx": "Sticky site header with logo, desktop/mobile navigation using `useNavigate` and `isAuthenticated` from AuthContext, with login/logout/register actions pointing to correct `/player/*` routes.",
  "frontend/src/pages/TournamentsPage.tsx": "Public tournaments listing page using `useGetAllTournaments` and `isAuthenticated`, with tournament cards showing entry fee/prize/slots/map, register button opening PaymentModal, and correct `/player/login` and `/player` navigation routes.",
  "frontend/src/index.css": "Global styles with OKLCH color system, custom font utilities, scrollbar styling, and new colorful Terms page CSS classes — including gradient title text, rainbow bar, neon heading variants (orange/cyan/pink/gold/lime/purple), and matching paragraph/bullet color variants."
}